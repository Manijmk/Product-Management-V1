// Generated by Prisma introspection over the authoritative SQL schema.
// The SQL migrations remain authoritative for RLS, triggers, constraints, and views.
generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["multiSchema"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
  schemas  = ["pms"]
}

model Tenant {
  tenantId           BigInt               @id @default(autoincrement()) @map("tenant_id")
  tenantCode         String               @unique @map("tenant_code") @db.VarChar(50)
  name               String               @db.VarChar(200)
  businessType       String               @map("business_type") @db.VarChar(50)
  status             String               @default("ACTIVE") @db.VarChar(20)
  defaultCurrency    String               @default("INR") @map("default_currency") @db.Char(3)
  timezone           String               @default("Asia/Kolkata") @db.VarChar(100)
  defaultLanguage    String               @default("en") @map("default_language") @db.VarChar(20)
  createdAt          DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime             @default(now()) @map("updated_at") @db.Timestamptz(6)
  app_user           AppUser[]
  inventory_location inventory_location[]
  party              Party[]
  product            Product[]
  role               role[]
  route              route[]
  staff              staff[]
  trip               Trip[]
  vehicle            vehicle[]

  @@map("tenant")
  @@schema("pms")
}

model AppUser {
  userId                                                                                                 BigInt                           @id @default(autoincrement()) @map("user_id")
  tenantId                                                                                               BigInt                           @map("tenant_id")
  loginIdentity                                                                                          String?                          @map("login_identity") @db.VarChar(255)
  mobile                                                                                                 String?                          @db.VarChar(30)
  email                                                                                                  String?                          @db.VarChar(255)
  displayName                                                                                            String?                          @map("display_name") @db.VarChar(200)
  status                                                                                                 String                           @default("ACTIVE") @db.VarChar(20)
  lastLoginAt                                                                                            DateTime?                        @map("last_login_at") @db.Timestamptz(6)
  createdByUserId                                                                                        BigInt?                          @map("created_by_user_id")
  createdAt                                                                                              DateTime                         @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                                                                                              DateTime                         @default(now()) @map("updated_at") @db.Timestamptz(6)
  app_user                                                                                               AppUser?                         @relation("app_userToapp_user", fields: [tenantId, createdByUserId], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_app_user_created_by")
  password_credential                                                                                    app_user_password_credential?
  other_app_user                                                                                         AppUser[]                        @relation("app_userToapp_user")
  tenant                                                                                                 Tenant                           @relation(fields: [tenantId], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_app_user_tenant")
  cash_handover_cash_handover_tenant_id_confirmed_by_user_idToapp_user                                   cash_handover[]                  @relation("cash_handover_tenant_id_confirmed_by_user_idToapp_user")
  cash_handover_cash_handover_tenant_id_to_user_idToapp_user                                             cash_handover[]                  @relation("cash_handover_tenant_id_to_user_idToapp_user")
  exchange_exception                                                                                     exchange_exception[]
  inventory_ledger_entry                                                                                 InventoryLedgerEntry[]
  inventory_location                                                                                     inventory_location[]
  money_ledger_entry                                                                                     MoneyLedgerEntry[]
  party_party_tenant_id_approved_by_user_idToapp_user                                                    Party[]                          @relation("party_tenant_id_approved_by_user_idToapp_user")
  party_party_tenant_id_created_by_user_idToapp_user                                                     Party[]                          @relation("party_tenant_id_created_by_user_idToapp_user")
  party_product                                                                                          party_product[]
  party_product_price_party_product_price_tenant_id_approved_by_user_idToapp_user                        party_product_price[]            @relation("party_product_price_tenant_id_approved_by_user_idToapp_user")
  party_product_price_party_product_price_tenant_id_created_by_user_idToapp_user                         party_product_price[]            @relation("party_product_price_tenant_id_created_by_user_idToapp_user")
  payment_allocation                                                                                     payment_allocation[]
  post_reconciliation_adjustment_post_reconciliation_adjustment_tenant_id_approved_by_user_idToapp_user  post_reconciliation_adjustment[] @relation("post_reconciliation_adjustment_tenant_id_approved_by_user_idToapp_user")
  post_reconciliation_adjustment_post_reconciliation_adjustment_tenant_id_requested_by_user_idToapp_user post_reconciliation_adjustment[] @relation("post_reconciliation_adjustment_tenant_id_requested_by_user_idToapp_user")
  price_override_price_override_tenant_id_approved_by_user_idToapp_user                                  price_override[]                 @relation("price_override_tenant_id_approved_by_user_idToapp_user")
  price_override_price_override_tenant_id_requested_by_user_idToapp_user                                 price_override[]                 @relation("price_override_tenant_id_requested_by_user_idToapp_user")
  product                                                                                                Product[]
  product_damage_rate                                                                                    product_damage_rate[]
  product_price                                                                                          product_price[]
  reconciliation_exception                                                                               reconciliation_exception[]
  route                                                                                                  route[]
  route_stop_product                                                                                     route_stop_product[]
  route_stop_template                                                                                    route_stop_template[]
  staff_staff_tenant_id_created_by_user_idToapp_user                                                     staff[]                          @relation("staff_tenant_id_created_by_user_idToapp_user")
  staff_staff_tenant_id_user_idToapp_user                                                                staff[]                          @relation("staff_tenant_id_user_idToapp_user")
  stop_event                                                                                             StopEvent[]
  stop_follow_up                                                                                         stop_follow_up[]
  trip                                                                                                   Trip[]
  trip_cash_reconciliation_trip_cash_reconciliation_tenant_id_approved_by_user_idToapp_user              trip_cash_reconciliation[]       @relation("trip_cash_reconciliation_tenant_id_approved_by_user_idToapp_user")
  trip_cash_reconciliation_trip_cash_reconciliation_tenant_id_submitted_by_user_idToapp_user             trip_cash_reconciliation[]       @relation("trip_cash_reconciliation_tenant_id_submitted_by_user_idToapp_user")
  trip_reconciliation_trip_reconciliation_tenant_id_approved_by_user_idToapp_user                        trip_reconciliation[]            @relation("trip_reconciliation_tenant_id_approved_by_user_idToapp_user")
  trip_reconciliation_trip_reconciliation_tenant_id_reopened_by_user_idToapp_user                        trip_reconciliation[]            @relation("trip_reconciliation_tenant_id_reopened_by_user_idToapp_user")
  trip_reconciliation_trip_reconciliation_tenant_id_submitted_by_user_idToapp_user                       trip_reconciliation[]            @relation("trip_reconciliation_tenant_id_submitted_by_user_idToapp_user")
  trip_staff                                                                                             trip_staff[]
  trip_stock_reconciliation_trip_stock_reconciliation_tenant_id_approved_by_user_idToapp_user            trip_stock_reconciliation[]      @relation("trip_stock_reconciliation_tenant_id_approved_by_user_idToapp_user")
  trip_stock_reconciliation_trip_stock_reconciliation_tenant_id_counted_by_user_idToapp_user             trip_stock_reconciliation[]      @relation("trip_stock_reconciliation_tenant_id_counted_by_user_idToapp_user")
  trip_stop                                                                                              trip_stop[]
  user_role_user_role_tenant_id_assigned_by_user_idToapp_user                                            user_role[]                      @relation("user_role_tenant_id_assigned_by_user_idToapp_user")
  user_role_user_role_tenant_id_user_idToapp_user                                                        user_role[]                      @relation("user_role_tenant_id_user_idToapp_user")
  vehicle                                                                                                vehicle[]

  @@unique([tenantId, userId], map: "uq_app_user_tenant_user")
  @@index([tenantId, status], map: "ix_app_user_tenant_status")
  @@map("app_user")
  @@schema("pms")
}

model app_user_password_credential {
  tenant_id            BigInt
  user_id              BigInt
  password_hash        String   @db.VarChar(512)
  password_changed_at  DateTime @default(now()) @db.Timestamptz(6)
  failed_attempt_count Int      @default(0)
  locked_until         DateTime? @db.Timestamptz(6)
  created_at           DateTime @default(now()) @db.Timestamptz(6)
  updated_at           DateTime @default(now()) @db.Timestamptz(6)
  app_user             AppUser  @relation(fields: [tenant_id, user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_password_credential_user")

  @@id([tenant_id, user_id])
  @@map("app_user_password_credential")
  @@schema("pms")
}

model Product {
  productId                 BigInt                      @id @default(autoincrement()) @map("product_id")
  tenantId                  BigInt                      @map("tenant_id")
  productCode               String                      @map("product_code") @db.VarChar(50)
  name                      String                      @db.VarChar(200)
  unitType                  String                      @map("unit_type") @db.VarChar(20)
  exchangeRatio             Decimal?                    @map("exchange_ratio") @db.Decimal(12, 4)
  activeStatus              String                      @default("ACTIVE") @map("active_status") @db.VarChar(20)
  createdByUserId           BigInt?                     @map("created_by_user_id")
  createdAt                 DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                 DateTime                    @default(now()) @map("updated_at") @db.Timestamptz(6)
  inventory_ledger_entry    InventoryLedgerEntry[]
  party_product             party_product[]
  party_product_price       party_product_price[]
  app_user                  AppUser?                    @relation(fields: [tenantId, createdByUserId], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_product_created_by")
  tenant                    Tenant                      @relation(fields: [tenantId], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_product_tenant")
  product_damage_rate       product_damage_rate[]
  product_price             product_price[]
  stop_event_product        stop_event_product[]
  stop_follow_up            stop_follow_up[]
  trip_stock_reconciliation trip_stock_reconciliation[]
  trip_stop_product         trip_stop_product[]

  @@unique([tenantId, productId], map: "uq_product_tenant_product")
  @@unique([tenantId, productCode], map: "uq_product_tenant_code")
  @@index([tenantId, activeStatus], map: "ix_product_tenant_status")
  @@map("product")
  @@schema("pms")
}

model Party {
  partyId                                                BigInt                 @id @default(autoincrement()) @map("party_id")
  tenantId                                               BigInt                 @map("tenant_id")
  party_code                                             String?                @db.VarChar(50)
  name                                                   String                 @db.VarChar(200)
  party_type                                             String                 @default("CUSTOMER") @db.VarChar(30)
  relationshipType                                       String                 @map("relationship_type") @db.VarChar(30)
  mobile                                                 String?                @db.VarChar(30)
  alternate_mobile                                       String?                @db.VarChar(30)
  address_line1                                          String?                @db.VarChar(255)
  address_line2                                          String?                @db.VarChar(255)
  locality                                               String?                @db.VarChar(150)
  city                                                   String?                @db.VarChar(100)
  postal_code                                            String?                @db.VarChar(20)
  latitude                                               Decimal?               @db.Decimal(9, 6)
  longitude                                              Decimal?               @db.Decimal(9, 6)
  customerStatus                                         String                 @default("PENDING_APPROVAL") @map("customer_status") @db.VarChar(30)
  createdSource                                          String                 @default("ADMIN") @map("created_source") @db.VarChar(30)
  createdByUserId                                        BigInt?                @map("created_by_user_id")
  approvedByUserId                                       BigInt?                @map("approved_by_user_id")
  approvedAt                                             DateTime?              @map("approved_at") @db.Timestamptz(6)
  createdAt                                              DateTime               @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                                              DateTime               @default(now()) @map("updated_at") @db.Timestamptz(6)
  inventory_ledger_entry                                 InventoryLedgerEntry[]
  inventory_location                                     inventory_location[]
  money_ledger_entry                                     MoneyLedgerEntry[]
  app_user_party_tenant_id_approved_by_user_idToapp_user AppUser?               @relation("party_tenant_id_approved_by_user_idToapp_user", fields: [tenantId, approvedByUserId], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_approved_by")
  app_user_party_tenant_id_created_by_user_idToapp_user  AppUser?               @relation("party_tenant_id_created_by_user_idToapp_user", fields: [tenantId, createdByUserId], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_created_by")
  tenant                                                 Tenant                 @relation(fields: [tenantId], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_tenant")
  party_product                                          party_product[]
  party_product_price                                    party_product_price[]
  route_stop_template                                    route_stop_template[]
  stop_event                                             StopEvent[]
  stop_follow_up                                         stop_follow_up[]
  trip_stop                                              trip_stop[]

  @@unique([tenantId, partyId], map: "uq_party_tenant_party")
  @@index([tenantId, mobile], map: "ix_party_tenant_mobile")
  @@index([tenantId, customerStatus], map: "ix_party_tenant_status")
  @@map("party")
  @@schema("pms")
}

model Trip {
  tripId                         BigInt                           @id @default(autoincrement()) @map("trip_id")
  tenantId                       BigInt                           @map("tenant_id")
  tripNumber                     String                           @map("trip_number") @db.VarChar(80)
  routeId                        BigInt?                          @map("route_id")
  tripDate                       DateTime                         @map("trip_date") @db.Date
  shift_code                     String?                          @db.VarChar(30)
  vehicleId                      BigInt?                          @map("vehicle_id")
  primaryStaffId                 BigInt?                          @map("primary_staff_id")
  status                         String                           @default("PLANNED") @db.VarChar(20)
  planned_start_at               DateTime?                        @db.Timestamptz(6)
  actual_start_at                DateTime?                        @db.Timestamptz(6)
  completed_at                   DateTime?                        @db.Timestamptz(6)
  reconciled_at                  DateTime?                        @db.Timestamptz(6)
  cancelled_at                   DateTime?                        @db.Timestamptz(6)
  cancellation_reason            String?                          @db.VarChar(500)
  created_by_user_id             BigInt?
  createdAt                      DateTime                         @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                      DateTime                         @default(now()) @map("updated_at") @db.Timestamptz(6)
  cash_handover                  cash_handover[]
  inventory_ledger_entry         InventoryLedgerEntry[]
  money_ledger_entry             MoneyLedgerEntry[]
  post_reconciliation_adjustment post_reconciliation_adjustment[]
  reconciliation_exception       reconciliation_exception[]
  stop_event                     StopEvent[]
  app_user                       AppUser?                         @relation(fields: [tenantId, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_created_by")
  staff                          staff?                           @relation(fields: [tenantId, primaryStaffId], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_primary_staff")
  route                          route?                           @relation(fields: [tenantId, routeId], references: [tenant_id, route_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_route")
  tenant                         Tenant                           @relation(fields: [tenantId], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_tenant")
  vehicle                        vehicle?                         @relation(fields: [tenantId, vehicleId], references: [tenant_id, vehicle_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_vehicle")
  trip_reconciliation            trip_reconciliation?
  trip_staff                     trip_staff[]
  trip_stop                      trip_stop[]

  @@unique([tenantId, tripId], map: "uq_trip_tenant_trip")
  @@unique([tenantId, tripNumber], map: "uq_trip_tenant_number")
  @@index([tenantId, tripDate, status], map: "ix_trip_tenant_date_status")
  @@index([tenantId, primaryStaffId, tripDate], map: "ix_trip_tenant_staff_date")
  @@index([tenantId, vehicleId, tripDate], map: "ix_trip_tenant_vehicle_date")
  @@map("trip")
  @@schema("pms")
}

model StopEvent {
  stopEventId                    BigInt                           @id @default(autoincrement()) @map("stop_event_id")
  tenantId                       BigInt                           @map("tenant_id")
  tripId                         BigInt                           @map("trip_id")
  tripStopId                     BigInt                           @map("trip_stop_id")
  partyId                        BigInt                           @map("party_id")
  eventType                      String                           @map("event_type") @db.VarChar(30)
  eventStatus                    String                           @default("POSTED") @map("event_status") @db.VarChar(20)
  performed_by_staff_id          BigInt?
  performed_by_user_id           BigInt?
  eventTime                      DateTime                         @map("event_time") @db.Timestamptz(6)
  server_received_at             DateTime                         @default(now()) @db.Timestamptz(6)
  clientUuid                     String                           @map("client_uuid") @db.Uuid
  device_id                      String?                          @db.VarChar(150)
  latitude                       Decimal?                         @db.Decimal(9, 6)
  longitude                      Decimal?                         @db.Decimal(9, 6)
  notes                          String?                          @db.VarChar(1000)
  correctionOfEventId            BigInt?                          @map("correction_of_event_id")
  correction_reason              String?                          @db.VarChar(500)
  createdAt                      DateTime                         @default(now()) @map("created_at") @db.Timestamptz(6)
  inventory_ledger_entry         InventoryLedgerEntry[]
  money_ledger_entry             MoneyLedgerEntry[]
  post_reconciliation_adjustment post_reconciliation_adjustment[]
  reconciliation_exception       reconciliation_exception[]
  stop_event                     StopEvent?                       @relation("stop_eventTostop_event", fields: [tenantId, correctionOfEventId], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_correction")
  other_stop_event               StopEvent[]                      @relation("stop_eventTostop_event")
  party                          Party                            @relation(fields: [tenantId, partyId], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_party")
  staff                          staff?                           @relation(fields: [tenantId, performed_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_staff")
  trip                           Trip                             @relation(fields: [tenantId, tripId], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_trip")
  trip_stop                      trip_stop                        @relation(fields: [tenantId, tripStopId], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_trip_stop")
  app_user                       AppUser?                         @relation(fields: [tenantId, performed_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_user")
  stop_event_product             stop_event_product[]
  stop_follow_up                 stop_follow_up[]

  @@unique([tenantId, stopEventId], map: "uq_stop_event_tenant_id")
  @@unique([tenantId, clientUuid], map: "uq_stop_event_client_uuid")
  @@index([tenantId, partyId, eventTime(sort: Desc)], map: "ix_stop_event_party_time")
  @@index([tenantId, tripStopId, eventTime], map: "ix_stop_event_trip_stop_time")
  @@index([tenantId, tripId, eventTime], map: "ix_stop_event_trip_time")
  @@map("stop_event")
  @@schema("pms")
}

model InventoryLedgerEntry {
  inventoryLedgerId                                                                        BigInt                           @id @default(autoincrement()) @map("inventory_ledger_id")
  tenantId                                                                                 BigInt                           @map("tenant_id")
  productId                                                                                BigInt                           @map("product_id")
  from_location_id                                                                         BigInt?
  to_location_id                                                                           BigInt?
  from_state_id                                                                            BigInt?
  to_state_id                                                                              BigInt?
  party_id                                                                                 BigInt?
  quantity                                                                                 Decimal                          @db.Decimal(14, 3)
  eventType                                                                                String                           @map("event_type") @db.VarChar(40)
  tripId                                                                                   BigInt?                          @map("trip_id")
  trip_stop_id                                                                             BigInt?
  stopEventId                                                                              BigInt?                          @map("stop_event_id")
  reference_type                                                                           String?                          @db.VarChar(50)
  reference_id                                                                             BigInt?
  reverses_inventory_ledger_id                                                             BigInt?
  occurredAt                                                                               DateTime                         @map("occurred_at") @db.Timestamptz(6)
  created_by_user_id                                                                       BigInt?
  created_by_staff_id                                                                      BigInt?
  createdAt                                                                                DateTime                         @default(now()) @map("created_at") @db.Timestamptz(6)
  staff                                                                                    staff?                           @relation(fields: [tenantId, created_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_created_by_staff")
  app_user                                                                                 AppUser?                         @relation(fields: [tenantId, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_created_by_user")
  inventory_location_inventory_ledger_entry_tenant_id_from_location_idToinventory_location inventory_location?              @relation("inventory_ledger_entry_tenant_id_from_location_idToinventory_location", fields: [tenantId, from_location_id], references: [tenant_id, inventory_location_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_from_location")
  inventory_state_inventory_ledger_entry_from_state_idToinventory_state                    inventory_state?                 @relation("inventory_ledger_entry_from_state_idToinventory_state", fields: [from_state_id], references: [inventory_state_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_from_state")
  party                                                                                    Party?                           @relation(fields: [tenantId, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_party")
  product                                                                                  Product                          @relation(fields: [tenantId, productId], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_product")
  inventory_ledger_entry                                                                   InventoryLedgerEntry?            @relation("inventory_ledger_entryToinventory_ledger_entry", fields: [tenantId, reverses_inventory_ledger_id], references: [tenantId, inventoryLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_reversal")
  other_inventory_ledger_entry                                                             InventoryLedgerEntry[]           @relation("inventory_ledger_entryToinventory_ledger_entry")
  stop_event                                                                               StopEvent?                       @relation(fields: [tenantId, stopEventId], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_stop_event")
  inventory_location_inventory_ledger_entry_tenant_id_to_location_idToinventory_location   inventory_location?              @relation("inventory_ledger_entry_tenant_id_to_location_idToinventory_location", fields: [tenantId, to_location_id], references: [tenant_id, inventory_location_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_to_location")
  inventory_state_inventory_ledger_entry_to_state_idToinventory_state                      inventory_state?                 @relation("inventory_ledger_entry_to_state_idToinventory_state", fields: [to_state_id], references: [inventory_state_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_to_state")
  trip                                                                                     Trip?                            @relation(fields: [tenantId, tripId], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_trip")
  trip_stop                                                                                trip_stop?                       @relation(fields: [tenantId, trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_ledger_trip_stop")
  post_reconciliation_adjustment                                                           post_reconciliation_adjustment[]
  trip_stock_reconciliation                                                                trip_stock_reconciliation[]

  @@unique([tenantId, inventoryLedgerId], map: "uq_inventory_ledger_tenant_id")
  @@index([tenantId, from_location_id, productId, occurredAt(sort: Desc)], map: "ix_inventory_ledger_from_location")
  @@index([tenantId, productId, occurredAt(sort: Desc)], map: "ix_inventory_ledger_product_time")
  @@index([tenantId, stopEventId], map: "ix_inventory_ledger_stop_event")
  @@index([tenantId, to_location_id, productId, occurredAt(sort: Desc)], map: "ix_inventory_ledger_to_location")
  @@index([tenantId, tripId, occurredAt], map: "ix_inventory_ledger_trip")
  @@map("inventory_ledger_entry")
  @@schema("pms")
}

model MoneyLedgerEntry {
  moneyLedgerId                                                                               BigInt                           @id @default(autoincrement()) @map("money_ledger_id")
  tenantId                                                                                    BigInt                           @map("tenant_id")
  partyId                                                                                     BigInt?                          @map("party_id")
  staffId                                                                                     BigInt?                          @map("staff_id")
  amount                                                                                      Decimal                          @db.Decimal(14, 2)
  direction                                                                                   String                           @db.VarChar(10)
  transactionType                                                                             String                           @map("transaction_type") @db.VarChar(40)
  payment_method                                                                              String?                          @db.VarChar(30)
  account_type                                                                                String?                          @db.VarChar(30)
  tripId                                                                                      BigInt?                          @map("trip_id")
  trip_stop_id                                                                                BigInt?
  stopEventId                                                                                 BigInt?                          @map("stop_event_id")
  invoice_id                                                                                  BigInt?
  reference_type                                                                              String?                          @db.VarChar(50)
  reference_id                                                                                BigInt?
  reverses_money_ledger_id                                                                    BigInt?
  occurredAt                                                                                  DateTime                         @map("occurred_at") @db.Timestamptz(6)
  created_by_user_id                                                                          BigInt?
  created_by_staff_id                                                                         BigInt?
  createdAt                                                                                   DateTime                         @default(now()) @map("created_at") @db.Timestamptz(6)
  cash_handover                                                                               cash_handover[]
  staff_money_ledger_entry_tenant_id_created_by_staff_idTostaff                               staff?                           @relation("money_ledger_entry_tenant_id_created_by_staff_idTostaff", fields: [tenantId, created_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_created_by_staff")
  app_user                                                                                    AppUser?                         @relation(fields: [tenantId, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_created_by_user")
  party                                                                                       Party?                           @relation(fields: [tenantId, partyId], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_party")
  money_ledger_entry                                                                          MoneyLedgerEntry?                @relation("money_ledger_entryTomoney_ledger_entry", fields: [tenantId, reverses_money_ledger_id], references: [tenantId, moneyLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_reversal")
  other_money_ledger_entry                                                                    MoneyLedgerEntry[]               @relation("money_ledger_entryTomoney_ledger_entry")
  staff_money_ledger_entry_tenant_id_staff_idTostaff                                          staff?                           @relation("money_ledger_entry_tenant_id_staff_idTostaff", fields: [tenantId, staffId], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_staff")
  stop_event                                                                                  StopEvent?                       @relation(fields: [tenantId, stopEventId], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_stop_event")
  trip                                                                                        Trip?                            @relation(fields: [tenantId, tripId], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_trip")
  trip_stop                                                                                   trip_stop?                       @relation(fields: [tenantId, trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_money_ledger_trip_stop")
  payment_allocation_payment_allocation_tenant_id_charge_money_ledger_idTomoney_ledger_entry  payment_allocation[]             @relation("payment_allocation_tenant_id_charge_money_ledger_idTomoney_ledger_entry")
  payment_allocation_payment_allocation_tenant_id_payment_money_ledger_idTomoney_ledger_entry payment_allocation[]             @relation("payment_allocation_tenant_id_payment_money_ledger_idTomoney_ledger_entry")
  post_reconciliation_adjustment                                                              post_reconciliation_adjustment[]
  trip_cash_reconciliation                                                                    trip_cash_reconciliation[]

  @@unique([tenantId, moneyLedgerId], map: "uq_money_ledger_tenant_id")
  @@index([tenantId, partyId, occurredAt(sort: Desc)], map: "ix_money_ledger_party_time")
  @@index([tenantId, staffId, occurredAt(sort: Desc)], map: "ix_money_ledger_staff_time")
  @@index([tenantId, stopEventId], map: "ix_money_ledger_stop_event")
  @@index([tenantId, tripId, occurredAt], map: "ix_money_ledger_trip")
  @@map("money_ledger_entry")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model cash_handover {
  cash_handover_id                                                BigInt               @id @default(autoincrement())
  tenant_id                                                       BigInt
  trip_id                                                         BigInt?
  reconciliation_id                                               BigInt?
  from_staff_id                                                   BigInt
  to_user_id                                                      BigInt
  amount                                                          Decimal              @db.Decimal(14, 2)
  status                                                          String               @default("SUBMITTED") @db.VarChar(20)
  submitted_at                                                    DateTime             @default(now()) @db.Timestamptz(6)
  confirmed_at                                                    DateTime?            @db.Timestamptz(6)
  disputed_at                                                     DateTime?            @db.Timestamptz(6)
  submitted_by_staff_id                                           BigInt
  confirmed_by_user_id                                            BigInt?
  dispute_reason                                                  String?              @db.VarChar(1000)
  money_ledger_id                                                 BigInt?
  created_at                                                      DateTime             @default(now()) @db.Timestamptz(6)
  app_user_cash_handover_tenant_id_confirmed_by_user_idToapp_user AppUser?             @relation("cash_handover_tenant_id_confirmed_by_user_idToapp_user", fields: [tenant_id, confirmed_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_confirmed_user")
  staff_cash_handover_tenant_id_from_staff_idTostaff              staff                @relation("cash_handover_tenant_id_from_staff_idTostaff", fields: [tenant_id, from_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_from_staff")
  money_ledger_entry                                              MoneyLedgerEntry?    @relation(fields: [tenant_id, money_ledger_id], references: [tenantId, moneyLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_money_ledger")
  trip_reconciliation                                             trip_reconciliation? @relation(fields: [tenant_id, reconciliation_id], references: [tenant_id, reconciliation_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_reconciliation")
  staff_cash_handover_tenant_id_submitted_by_staff_idTostaff      staff                @relation("cash_handover_tenant_id_submitted_by_staff_idTostaff", fields: [tenant_id, submitted_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_submitted_staff")
  app_user_cash_handover_tenant_id_to_user_idToapp_user           AppUser              @relation("cash_handover_tenant_id_to_user_idToapp_user", fields: [tenant_id, to_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_to_user")
  trip                                                            Trip?                @relation(fields: [tenant_id, trip_id], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_cash_handover_trip")

  @@unique([tenant_id, cash_handover_id], map: "uq_cash_handover_tenant_id")
  @@index([tenant_id, from_staff_id, submitted_at(sort: Desc)], map: "ix_cash_handover_staff")
  @@index([tenant_id, trip_id, status], map: "ix_cash_handover_trip")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model exchange_exception {
  exchange_exception_id  BigInt             @id @default(autoincrement())
  tenant_id              BigInt
  stop_event_product_id  BigInt
  exception_type         String             @db.VarChar(40)
  quantity               Decimal            @db.Decimal(14, 3)
  resolution             String             @db.VarChar(50)
  authorized_by_staff_id BigInt?
  approved_by_user_id    BigInt?
  approved_at            DateTime?          @db.Timestamptz(6)
  notes                  String?            @db.VarChar(1000)
  created_at             DateTime           @default(now()) @db.Timestamptz(6)
  stop_event_product     stop_event_product @relation(fields: [tenant_id, stop_event_product_id], references: [tenant_id, stop_event_product_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_exchange_exception_product")
  staff                  staff?             @relation(fields: [tenant_id, authorized_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_exchange_exception_staff")
  app_user               AppUser?           @relation(fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_exchange_exception_user")

  @@unique([tenant_id, exchange_exception_id], map: "uq_exchange_exception_tenant_id")
  @@index([tenant_id, stop_event_product_id], map: "ix_exchange_exception_event_product")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model inventory_location {
  inventory_location_id                                                                        BigInt                      @id @default(autoincrement())
  tenant_id                                                                                    BigInt
  location_code                                                                                String                      @db.VarChar(60)
  name                                                                                         String                      @db.VarChar(200)
  location_type                                                                                String                      @db.VarChar(30)
  vehicle_id                                                                                   BigInt?
  party_id                                                                                     BigInt?
  status                                                                                       String                      @default("ACTIVE") @db.VarChar(20)
  created_by_user_id                                                                           BigInt?
  created_at                                                                                   DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at                                                                                   DateTime                    @default(now()) @db.Timestamptz(6)
  inventory_ledger_entry_inventory_ledger_entry_tenant_id_from_location_idToinventory_location InventoryLedgerEntry[]      @relation("inventory_ledger_entry_tenant_id_from_location_idToinventory_location")
  inventory_ledger_entry_inventory_ledger_entry_tenant_id_to_location_idToinventory_location   InventoryLedgerEntry[]      @relation("inventory_ledger_entry_tenant_id_to_location_idToinventory_location")
  app_user                                                                                     AppUser?                    @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_location_created_by")
  party                                                                                        Party?                      @relation(fields: [tenant_id, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_location_party")
  tenant                                                                                       Tenant                      @relation(fields: [tenant_id], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_location_tenant")
  vehicle                                                                                      vehicle?                    @relation(fields: [tenant_id, vehicle_id], references: [tenant_id, vehicle_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_inventory_location_vehicle")
  trip_stock_reconciliation                                                                    trip_stock_reconciliation[]

  @@unique([tenant_id, location_code], map: "uq_inventory_location_code")
  @@unique([tenant_id, inventory_location_id], map: "uq_inventory_location_tenant_id")
  @@index([tenant_id, location_type, status], map: "ix_inventory_location_type")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model inventory_state {
  inventory_state_id                                                           BigInt                      @id @default(autoincrement())
  code                                                                         String                      @unique @db.VarChar(30)
  name                                                                         String                      @db.VarChar(100)
  is_available_for_delivery                                                    Boolean                     @default(false)
  is_exchange_eligible                                                         Boolean                     @default(false)
  sort_order                                                                   Int                         @default(0)
  active_status                                                                String                      @default("ACTIVE") @db.VarChar(20)
  inventory_ledger_entry_inventory_ledger_entry_from_state_idToinventory_state InventoryLedgerEntry[]      @relation("inventory_ledger_entry_from_state_idToinventory_state")
  inventory_ledger_entry_inventory_ledger_entry_to_state_idToinventory_state   InventoryLedgerEntry[]      @relation("inventory_ledger_entry_to_state_idToinventory_state")
  trip_stock_reconciliation                                                    trip_stock_reconciliation[]

  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model party_product {
  party_product_id   BigInt               @id @default(autoincrement())
  tenant_id          BigInt
  party_id           BigInt
  product_id         BigInt
  quantity_mode      String               @db.VarChar(30)
  default_qty        Decimal?             @db.Decimal(14, 3)
  forecast_qty       Decimal?             @db.Decimal(14, 3)
  exchange_policy    String?              @db.VarChar(30)
  active_status      String               @default("ACTIVE") @db.VarChar(20)
  created_by_user_id BigInt?
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  app_user           AppUser?             @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_created_by")
  party              Party                @relation(fields: [tenant_id, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_party")
  product            Product              @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_product")
  route_stop_product route_stop_product[]
  trip_stop_product  trip_stop_product[]

  @@unique([tenant_id, party_id, product_id], map: "uq_party_product_customer_product")
  @@unique([tenant_id, party_product_id], map: "uq_party_product_tenant_id")
  @@index([tenant_id, party_id, active_status], map: "ix_party_product_lookup")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model party_product_price {
  party_product_price_id                                               BigInt    @id @default(autoincrement())
  tenant_id                                                            BigInt
  party_id                                                             BigInt
  product_id                                                           BigInt
  price                                                                Decimal   @db.Decimal(14, 2)
  effective_from                                                       DateTime  @db.Timestamptz(6)
  effective_to                                                         DateTime? @db.Timestamptz(6)
  approval_status                                                      String    @default("APPROVED") @db.VarChar(20)
  created_by_user_id                                                   BigInt?
  approved_by_user_id                                                  BigInt?
  approved_at                                                          DateTime? @db.Timestamptz(6)
  created_at                                                           DateTime  @default(now()) @db.Timestamptz(6)
  app_user_party_product_price_tenant_id_approved_by_user_idToapp_user AppUser?  @relation("party_product_price_tenant_id_approved_by_user_idToapp_user", fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_price_approved_by")
  app_user_party_product_price_tenant_id_created_by_user_idToapp_user  AppUser?  @relation("party_product_price_tenant_id_created_by_user_idToapp_user", fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_price_created_by")
  party                                                                Party     @relation(fields: [tenant_id, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_price_party")
  product                                                              Product   @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_party_product_price_product")

  @@unique([tenant_id, party_product_price_id], map: "uq_party_product_price_tenant_id")
  @@index([tenant_id, party_id, product_id, effective_from(sort: Desc)], map: "ix_party_product_price_lookup")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model payment_allocation {
  payment_allocation_id                                                                       BigInt           @id @default(autoincrement())
  tenant_id                                                                                   BigInt
  payment_money_ledger_id                                                                     BigInt
  charge_money_ledger_id                                                                      BigInt
  allocated_amount                                                                            Decimal          @db.Decimal(14, 2)
  created_by_user_id                                                                          BigInt?
  created_by_staff_id                                                                         BigInt?
  created_at                                                                                  DateTime         @default(now()) @db.Timestamptz(6)
  money_ledger_entry_payment_allocation_tenant_id_charge_money_ledger_idTomoney_ledger_entry  MoneyLedgerEntry @relation("payment_allocation_tenant_id_charge_money_ledger_idTomoney_ledger_entry", fields: [tenant_id, charge_money_ledger_id], references: [tenantId, moneyLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_payment_allocation_charge")
  staff                                                                                       staff?           @relation(fields: [tenant_id, created_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_payment_allocation_created_by_staff")
  app_user                                                                                    AppUser?         @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_payment_allocation_created_by_user")
  money_ledger_entry_payment_allocation_tenant_id_payment_money_ledger_idTomoney_ledger_entry MoneyLedgerEntry @relation("payment_allocation_tenant_id_payment_money_ledger_idTomoney_ledger_entry", fields: [tenant_id, payment_money_ledger_id], references: [tenantId, moneyLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_payment_allocation_payment")

  @@unique([tenant_id, payment_money_ledger_id, charge_money_ledger_id], map: "uq_payment_allocation_pair")
  @@unique([tenant_id, payment_allocation_id], map: "uq_payment_allocation_tenant_id")
  @@index([tenant_id, charge_money_ledger_id], map: "ix_payment_allocation_charge")
  @@index([tenant_id, payment_money_ledger_id], map: "ix_payment_allocation_payment")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model post_reconciliation_adjustment {
  post_recon_adjustment_id                                                         BigInt                    @id @default(autoincrement())
  tenant_id                                                                        BigInt
  reconciliation_id                                                                BigInt
  trip_id                                                                          BigInt
  reconciliation_exception_id                                                      BigInt?
  adjustment_type                                                                  String                    @db.VarChar(30)
  reason_code                                                                      String                    @db.VarChar(50)
  reason_notes                                                                     String?                   @db.VarChar(1500)
  requested_by_staff_id                                                            BigInt?
  requested_by_user_id                                                             BigInt?
  requested_at                                                                     DateTime                  @default(now()) @db.Timestamptz(6)
  approval_status                                                                  String                    @default("PENDING") @db.VarChar(20)
  approved_by_user_id                                                              BigInt?
  approved_at                                                                      DateTime?                 @db.Timestamptz(6)
  inventory_ledger_id                                                              BigInt?
  money_ledger_id                                                                  BigInt?
  stop_event_id                                                                    BigInt?
  created_at                                                                       DateTime                  @default(now()) @db.Timestamptz(6)
  app_user_post_reconciliation_adjustment_tenant_id_approved_by_user_idToapp_user  AppUser?                  @relation("post_reconciliation_adjustment_tenant_id_approved_by_user_idToapp_user", fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_approved_user")
  reconciliation_exception                                                         reconciliation_exception? @relation(fields: [tenant_id, reconciliation_exception_id], references: [tenant_id, reconciliation_exception_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_exception")
  trip_reconciliation                                                              trip_reconciliation       @relation(fields: [tenant_id, reconciliation_id], references: [tenant_id, reconciliation_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_header")
  inventory_ledger_entry                                                           InventoryLedgerEntry?     @relation(fields: [tenant_id, inventory_ledger_id], references: [tenantId, inventoryLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_inventory_ledger")
  money_ledger_entry                                                               MoneyLedgerEntry?         @relation(fields: [tenant_id, money_ledger_id], references: [tenantId, moneyLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_money_ledger")
  staff                                                                            staff?                    @relation(fields: [tenant_id, requested_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_requested_staff")
  app_user_post_reconciliation_adjustment_tenant_id_requested_by_user_idToapp_user AppUser?                  @relation("post_reconciliation_adjustment_tenant_id_requested_by_user_idToapp_user", fields: [tenant_id, requested_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_requested_user")
  stop_event                                                                       StopEvent?                @relation(fields: [tenant_id, stop_event_id], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_stop_event")
  trip                                                                             Trip                      @relation(fields: [tenant_id, trip_id], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_post_recon_adjustment_trip")

  @@unique([tenant_id, post_recon_adjustment_id], map: "uq_post_recon_adjustment_tenant_id")
  @@index([tenant_id, reconciliation_id, approval_status], map: "ix_post_recon_adjustment_header")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model price_override {
  price_override_id                                                BigInt             @id @default(autoincrement())
  tenant_id                                                        BigInt
  stop_event_product_id                                            BigInt
  standard_price                                                   Decimal            @db.Decimal(14, 2)
  requested_price                                                  Decimal            @db.Decimal(14, 2)
  requested_by_staff_id                                            BigInt?
  requested_by_user_id                                             BigInt?
  approval_status                                                  String             @default("PENDING") @db.VarChar(20)
  approved_by_user_id                                              BigInt?
  approved_at                                                      DateTime?          @db.Timestamptz(6)
  reason                                                           String             @db.VarChar(1000)
  created_at                                                       DateTime           @default(now()) @db.Timestamptz(6)
  app_user_price_override_tenant_id_approved_by_user_idToapp_user  AppUser?           @relation("price_override_tenant_id_approved_by_user_idToapp_user", fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_price_override_approved_by")
  stop_event_product                                               stop_event_product @relation(fields: [tenant_id, stop_event_product_id], references: [tenant_id, stop_event_product_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_price_override_event_product")
  staff                                                            staff?             @relation(fields: [tenant_id, requested_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_price_override_requested_by_staff")
  app_user_price_override_tenant_id_requested_by_user_idToapp_user AppUser?           @relation("price_override_tenant_id_requested_by_user_idToapp_user", fields: [tenant_id, requested_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_price_override_requested_by_user")

  @@unique([tenant_id, stop_event_product_id], map: "uq_price_override_event_product")
  @@unique([tenant_id, price_override_id], map: "uq_price_override_tenant_id")
  @@index([tenant_id, approval_status, created_at(sort: Desc)], map: "ix_price_override_status")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_damage_rate {
  damage_rate_id     BigInt    @id @default(autoincrement())
  tenant_id          BigInt
  product_id         BigInt
  damage_type        String    @db.VarChar(30)
  rate               Decimal   @db.Decimal(14, 2)
  effective_from     DateTime  @db.Timestamptz(6)
  effective_to       DateTime? @db.Timestamptz(6)
  status             String    @default("ACTIVE") @db.VarChar(20)
  created_by_user_id BigInt?
  created_at         DateTime  @default(now()) @db.Timestamptz(6)
  app_user           AppUser?  @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_damage_rate_created_by")
  product            Product   @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_damage_rate_product")

  @@unique([tenant_id, damage_rate_id], map: "uq_damage_rate_tenant_id")
  @@index([tenant_id, product_id, damage_type, effective_from(sort: Desc)], map: "ix_damage_rate_lookup")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model product_price {
  product_price_id   BigInt    @id @default(autoincrement())
  tenant_id          BigInt
  product_id         BigInt
  price              Decimal   @db.Decimal(14, 2)
  effective_from     DateTime  @db.Timestamptz(6)
  effective_to       DateTime? @db.Timestamptz(6)
  status             String    @default("ACTIVE") @db.VarChar(20)
  created_by_user_id BigInt?
  created_at         DateTime  @default(now()) @db.Timestamptz(6)
  app_user           AppUser?  @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_product_price_created_by")
  product            Product   @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_product_price_product")

  @@unique([tenant_id, product_price_id], map: "uq_product_price_tenant_id")
  @@index([tenant_id, product_id, effective_from(sort: Desc)], map: "ix_product_price_lookup")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model reconciliation_exception {
  reconciliation_exception_id    BigInt                           @id @default(autoincrement())
  tenant_id                      BigInt
  reconciliation_id              BigInt
  exception_type                 String                           @db.VarChar(40)
  severity                       String                           @default("WARNING") @db.VarChar(20)
  trip_id                        BigInt
  trip_stop_id                   BigInt?
  stop_event_id                  BigInt?
  detected_at                    DateTime                         @default(now()) @db.Timestamptz(6)
  detected_source                String                           @db.VarChar(30)
  description                    String                           @db.VarChar(1500)
  status                         String                           @default("OPEN") @db.VarChar(20)
  resolved_by_user_id            BigInt?
  resolved_at                    DateTime?                        @db.Timestamptz(6)
  resolution_notes               String?                          @db.VarChar(1500)
  created_at                     DateTime                         @default(now()) @db.Timestamptz(6)
  post_reconciliation_adjustment post_reconciliation_adjustment[]
  trip_reconciliation            trip_reconciliation              @relation(fields: [tenant_id, reconciliation_id], references: [tenant_id, reconciliation_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_recon_exception_header")
  app_user                       AppUser?                         @relation(fields: [tenant_id, resolved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_recon_exception_resolved_by")
  stop_event                     StopEvent?                       @relation(fields: [tenant_id, stop_event_id], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_recon_exception_stop_event")
  trip                           Trip                             @relation(fields: [tenant_id, trip_id], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_recon_exception_trip")
  trip_stop                      trip_stop?                       @relation(fields: [tenant_id, trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_recon_exception_trip_stop")

  @@unique([tenant_id, reconciliation_exception_id], map: "uq_recon_exception_tenant_id")
  @@index([tenant_id, reconciliation_id, status, severity], map: "ix_recon_exception_open")
  @@index([tenant_id, trip_id, detected_at(sort: Desc)], map: "ix_recon_exception_trip")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role {
  role_id    BigInt      @id @default(autoincrement())
  tenant_id  BigInt
  role_code  String      @db.VarChar(50)
  role_name  String      @db.VarChar(100)
  status     String      @default("ACTIVE") @db.VarChar(20)
  created_at DateTime    @default(now()) @db.Timestamptz(6)
  updated_at DateTime    @default(now()) @db.Timestamptz(6)
  tenant     Tenant      @relation(fields: [tenant_id], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_role_tenant")
  user_role  user_role[]

  @@unique([tenant_id, role_code], map: "uq_role_tenant_code")
  @@unique([tenant_id, role_id], map: "uq_role_tenant_role")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model route {
  route_id            BigInt                @id @default(autoincrement())
  tenant_id           BigInt
  route_code          String                @db.VarChar(50)
  route_name          String                @db.VarChar(200)
  description         String?               @db.VarChar(500)
  status              String                @default("ACTIVE") @db.VarChar(20)
  created_by_user_id  BigInt?
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  updated_at          DateTime              @default(now()) @db.Timestamptz(6)
  app_user            AppUser?              @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_created_by")
  tenant              Tenant                @relation(fields: [tenant_id], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_tenant")
  route_stop_template route_stop_template[]
  trip                Trip[]

  @@unique([tenant_id, route_code], map: "uq_route_tenant_code")
  @@unique([tenant_id, route_id], map: "uq_route_tenant_route")
  @@index([tenant_id, status], map: "ix_route_tenant_status")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model route_stop_product {
  route_stop_product_id  BigInt              @id @default(autoincrement())
  tenant_id              BigInt
  route_stop_template_id BigInt
  party_product_id       BigInt
  quantity_mode          String              @db.VarChar(30)
  planned_qty            Decimal?            @db.Decimal(14, 3)
  forecast_qty           Decimal?            @db.Decimal(14, 3)
  notes                  String?             @db.VarChar(500)
  active_status          String              @default("ACTIVE") @db.VarChar(20)
  created_by_user_id     BigInt?
  created_at             DateTime            @default(now()) @db.Timestamptz(6)
  updated_at             DateTime            @default(now()) @db.Timestamptz(6)
  app_user               AppUser?            @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_stop_product_created_by")
  party_product          party_product       @relation(fields: [tenant_id, party_product_id], references: [tenant_id, party_product_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_stop_product_party_product")
  route_stop_template    route_stop_template @relation(fields: [tenant_id, route_stop_template_id], references: [tenant_id, route_stop_template_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_stop_product_template")

  @@unique([tenant_id, route_stop_template_id, party_product_id], map: "uq_route_stop_product_template_party_product")
  @@unique([tenant_id, route_stop_product_id], map: "uq_route_stop_product_tenant_id")
  @@index([tenant_id, route_stop_template_id, active_status], map: "ix_route_stop_product_template")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model route_stop_template {
  route_stop_template_id BigInt               @id @default(autoincrement())
  tenant_id              BigInt
  route_id               BigInt
  party_id               BigInt
  default_sequence       Decimal              @db.Decimal(18, 6)
  preferred_time_from    DateTime?            @db.Time(6)
  preferred_time_to      DateTime?            @db.Time(6)
  notes                  String?              @db.VarChar(500)
  active_status          String               @default("ACTIVE") @db.VarChar(20)
  created_by_user_id     BigInt?
  created_at             DateTime             @default(now()) @db.Timestamptz(6)
  updated_at             DateTime             @default(now()) @db.Timestamptz(6)
  route_stop_product     route_stop_product[]
  app_user               AppUser?             @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_stop_template_created_by")
  party                  Party                @relation(fields: [tenant_id, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_stop_template_party")
  route                  route                @relation(fields: [tenant_id, route_id], references: [tenant_id, route_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_route_stop_template_route")
  trip_stop              trip_stop[]

  @@unique([tenant_id, route_id, party_id], map: "uq_route_stop_template_route_party")
  @@unique([tenant_id, route_stop_template_id], map: "uq_route_stop_template_tenant_id")
  @@index([tenant_id, party_id, active_status], map: "ix_route_stop_template_party")
  @@index([tenant_id, route_id, default_sequence], map: "ix_route_stop_template_route_sequence")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model staff {
  staff_id                                                                                 BigInt                           @id @default(autoincrement())
  tenant_id                                                                                BigInt
  user_id                                                                                  BigInt?
  employee_code                                                                            String?                          @db.VarChar(50)
  name                                                                                     String                           @db.VarChar(200)
  mobile                                                                                   String?                          @db.VarChar(30)
  staff_type                                                                               String                           @db.VarChar(50)
  status                                                                                   String                           @default("ACTIVE") @db.VarChar(20)
  joined_on                                                                                DateTime?                        @db.Date
  created_by_user_id                                                                       BigInt?
  created_at                                                                               DateTime                         @default(now()) @db.Timestamptz(6)
  updated_at                                                                               DateTime                         @default(now()) @db.Timestamptz(6)
  cash_handover_cash_handover_tenant_id_from_staff_idTostaff                               cash_handover[]                  @relation("cash_handover_tenant_id_from_staff_idTostaff")
  cash_handover_cash_handover_tenant_id_submitted_by_staff_idTostaff                       cash_handover[]                  @relation("cash_handover_tenant_id_submitted_by_staff_idTostaff")
  exchange_exception                                                                       exchange_exception[]
  inventory_ledger_entry                                                                   InventoryLedgerEntry[]
  money_ledger_entry_money_ledger_entry_tenant_id_created_by_staff_idTostaff               MoneyLedgerEntry[]               @relation("money_ledger_entry_tenant_id_created_by_staff_idTostaff")
  money_ledger_entry_money_ledger_entry_tenant_id_staff_idTostaff                          MoneyLedgerEntry[]               @relation("money_ledger_entry_tenant_id_staff_idTostaff")
  payment_allocation                                                                       payment_allocation[]
  post_reconciliation_adjustment                                                           post_reconciliation_adjustment[]
  price_override                                                                           price_override[]
  app_user_staff_tenant_id_created_by_user_idToapp_user                                    AppUser?                         @relation("staff_tenant_id_created_by_user_idToapp_user", fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_staff_created_by")
  tenant                                                                                   Tenant                           @relation(fields: [tenant_id], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_staff_tenant")
  app_user_staff_tenant_id_user_idToapp_user                                               AppUser?                         @relation("staff_tenant_id_user_idToapp_user", fields: [tenant_id, user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_staff_user")
  stop_event                                                                               StopEvent[]
  stop_follow_up                                                                           stop_follow_up[]
  trip                                                                                     Trip[]
  trip_cash_reconciliation_trip_cash_reconciliation_tenant_id_staff_idTostaff              trip_cash_reconciliation[]       @relation("trip_cash_reconciliation_tenant_id_staff_idTostaff")
  trip_cash_reconciliation_trip_cash_reconciliation_tenant_id_submitted_by_staff_idTostaff trip_cash_reconciliation[]       @relation("trip_cash_reconciliation_tenant_id_submitted_by_staff_idTostaff")
  trip_reconciliation                                                                      trip_reconciliation[]
  trip_staff                                                                               trip_staff[]
  trip_stock_reconciliation                                                                trip_stock_reconciliation[]
  trip_stop                                                                                trip_stop[]

  @@unique([tenant_id, staff_id], map: "uq_staff_tenant_staff")
  @@index([tenant_id, status], map: "ix_staff_tenant_status")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model stop_event_product {
  stop_event_product_id      BigInt               @id @default(autoincrement())
  tenant_id                  BigInt
  stop_event_id              BigInt
  trip_stop_product_id       BigInt?
  product_id                 BigInt
  quantity_mode_snapshot     String               @db.VarChar(30)
  exchange_ratio_snapshot    Decimal?             @db.Decimal(12, 4)
  full_qty_delivered         Decimal              @default(0) @db.Decimal(14, 3)
  empty_qty_received_good    Decimal              @default(0) @db.Decimal(14, 3)
  empty_qty_received_damaged Decimal              @default(0) @db.Decimal(14, 3)
  empty_qty_rejected         Decimal              @default(0) @db.Decimal(14, 3)
  container_credit_qty       Decimal              @default(0) @db.Decimal(14, 3)
  container_due_qty          Decimal              @default(0) @db.Decimal(14, 3)
  price_applied              Decimal?             @db.Decimal(14, 2)
  price_source               String?              @db.VarChar(30)
  line_charge_amount         Decimal              @default(0) @db.Decimal(14, 2)
  damage_charge_amount       Decimal              @default(0) @db.Decimal(14, 2)
  notes                      String?              @db.VarChar(1000)
  created_at                 DateTime             @default(now()) @db.Timestamptz(6)
  exchange_exception         exchange_exception[]
  price_override             price_override?
  stop_event                 StopEvent            @relation(fields: [tenant_id, stop_event_id], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_product_event")
  product                    Product              @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_product_product")
  trip_stop_product          trip_stop_product?   @relation(fields: [tenant_id, trip_stop_product_id], references: [tenant_id, trip_stop_product_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_event_product_trip_stop_product")

  @@unique([tenant_id, stop_event_id, product_id], map: "uq_stop_event_product_event_product")
  @@unique([tenant_id, stop_event_product_id], map: "uq_stop_event_product_tenant_id")
  @@index([tenant_id, stop_event_id], map: "ix_stop_event_product_event")
  @@index([tenant_id, product_id, created_at(sort: Desc)], map: "ix_stop_event_product_product")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model stop_follow_up {
  follow_up_id                                                      BigInt     @id @default(autoincrement())
  tenant_id                                                         BigInt
  source_trip_stop_id                                               BigInt
  source_stop_event_id                                              BigInt?
  party_id                                                          BigInt
  product_id                                                        BigInt
  remaining_qty                                                     Decimal?   @db.Decimal(14, 3)
  resolution_type                                                   String     @db.VarChar(30)
  new_trip_stop_id                                                  BigInt?
  status                                                            String     @default("OPEN") @db.VarChar(20)
  reason                                                            String?    @db.VarChar(1000)
  created_by_user_id                                                BigInt?
  created_by_staff_id                                               BigInt?
  created_at                                                        DateTime   @default(now()) @db.Timestamptz(6)
  resolved_at                                                       DateTime?  @db.Timestamptz(6)
  staff                                                             staff?     @relation(fields: [tenant_id, created_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_created_by_staff")
  app_user                                                          AppUser?   @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_created_by_user")
  trip_stop_stop_follow_up_tenant_id_new_trip_stop_idTotrip_stop    trip_stop? @relation("stop_follow_up_tenant_id_new_trip_stop_idTotrip_stop", fields: [tenant_id, new_trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_new_stop")
  party                                                             Party      @relation(fields: [tenant_id, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_party")
  product                                                           Product    @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_product")
  stop_event                                                        StopEvent? @relation(fields: [tenant_id, source_stop_event_id], references: [tenantId, stopEventId], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_source_event")
  trip_stop_stop_follow_up_tenant_id_source_trip_stop_idTotrip_stop trip_stop  @relation("stop_follow_up_tenant_id_source_trip_stop_idTotrip_stop", fields: [tenant_id, source_trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_stop_follow_up_source_stop")

  @@unique([tenant_id, follow_up_id], map: "uq_stop_follow_up_tenant_id")
  @@index([tenant_id, status, party_id, created_at(sort: Desc)], map: "ix_stop_follow_up_open")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trip_cash_reconciliation {
  trip_cash_recon_id                                                         BigInt              @id @default(autoincrement())
  tenant_id                                                                  BigInt
  reconciliation_id                                                          BigInt
  staff_id                                                                   BigInt
  expected_cash                                                              Decimal             @db.Decimal(14, 2)
  actual_cash                                                                Decimal             @db.Decimal(14, 2)
  variance_amount                                                            Decimal?            @default(dbgenerated("(actual_cash - expected_cash)")) @db.Decimal(14, 2)
  variance_reason_code                                                       String?             @db.VarChar(50)
  variance_notes                                                             String?             @db.VarChar(1000)
  approval_status                                                            String              @default("NOT_REQUIRED") @db.VarChar(20)
  approved_by_user_id                                                        BigInt?
  approved_at                                                                DateTime?           @db.Timestamptz(6)
  adjustment_money_ledger_id                                                 BigInt?
  submitted_by_staff_id                                                      BigInt?
  submitted_by_user_id                                                       BigInt?
  submitted_at                                                               DateTime            @default(now()) @db.Timestamptz(6)
  created_at                                                                 DateTime            @default(now()) @db.Timestamptz(6)
  money_ledger_entry                                                         MoneyLedgerEntry?   @relation(fields: [tenant_id, adjustment_money_ledger_id], references: [tenantId, moneyLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_cash_recon_adjustment")
  app_user_trip_cash_reconciliation_tenant_id_approved_by_user_idToapp_user  AppUser?            @relation("trip_cash_reconciliation_tenant_id_approved_by_user_idToapp_user", fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_cash_recon_approved_by")
  trip_reconciliation                                                        trip_reconciliation @relation(fields: [tenant_id, reconciliation_id], references: [tenant_id, reconciliation_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_cash_recon_header")
  staff_trip_cash_reconciliation_tenant_id_staff_idTostaff                   staff               @relation("trip_cash_reconciliation_tenant_id_staff_idTostaff", fields: [tenant_id, staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_cash_recon_staff")
  staff_trip_cash_reconciliation_tenant_id_submitted_by_staff_idTostaff      staff?              @relation("trip_cash_reconciliation_tenant_id_submitted_by_staff_idTostaff", fields: [tenant_id, submitted_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_cash_recon_submit_staff")
  app_user_trip_cash_reconciliation_tenant_id_submitted_by_user_idToapp_user AppUser?            @relation("trip_cash_reconciliation_tenant_id_submitted_by_user_idToapp_user", fields: [tenant_id, submitted_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_cash_recon_submit_user")

  @@unique([tenant_id, reconciliation_id, staff_id], map: "uq_trip_cash_recon_staff")
  @@unique([tenant_id, trip_cash_recon_id], map: "uq_trip_cash_recon_tenant_id")
  @@index([tenant_id, reconciliation_id], map: "ix_trip_cash_recon_header")
  @@index([tenant_id, reconciliation_id, variance_amount], map: "ix_trip_cash_recon_variance")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trip_reconciliation {
  reconciliation_id                                                     BigInt                           @id @default(autoincrement())
  tenant_id                                                             BigInt
  trip_id                                                               BigInt
  reconciliation_number                                                 String                           @db.VarChar(80)
  status                                                                String                           @default("OPEN") @db.VarChar(20)
  started_at                                                            DateTime                         @default(now()) @db.Timestamptz(6)
  submitted_at                                                          DateTime?                        @db.Timestamptz(6)
  approved_at                                                           DateTime?                        @db.Timestamptz(6)
  reopened_at                                                           DateTime?                        @db.Timestamptz(6)
  submitted_by_staff_id                                                 BigInt?
  submitted_by_user_id                                                  BigInt?
  approved_by_user_id                                                   BigInt?
  reopened_by_user_id                                                   BigInt?
  notes                                                                 String?                          @db.VarChar(1000)
  created_at                                                            DateTime                         @default(now()) @db.Timestamptz(6)
  updated_at                                                            DateTime                         @default(now()) @db.Timestamptz(6)
  cash_handover                                                         cash_handover[]
  post_reconciliation_adjustment                                        post_reconciliation_adjustment[]
  reconciliation_exception                                              reconciliation_exception[]
  trip_cash_reconciliation                                              trip_cash_reconciliation[]
  app_user_trip_reconciliation_tenant_id_approved_by_user_idToapp_user  AppUser?                         @relation("trip_reconciliation_tenant_id_approved_by_user_idToapp_user", fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_recon_approved_user")
  app_user_trip_reconciliation_tenant_id_reopened_by_user_idToapp_user  AppUser?                         @relation("trip_reconciliation_tenant_id_reopened_by_user_idToapp_user", fields: [tenant_id, reopened_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_recon_reopened_user")
  staff                                                                 staff?                           @relation(fields: [tenant_id, submitted_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_recon_submitted_staff")
  app_user_trip_reconciliation_tenant_id_submitted_by_user_idToapp_user AppUser?                         @relation("trip_reconciliation_tenant_id_submitted_by_user_idToapp_user", fields: [tenant_id, submitted_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_recon_submitted_user")
  trip                                                                  Trip                             @relation(fields: [tenant_id, trip_id], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_recon_trip")
  trip_stock_reconciliation                                             trip_stock_reconciliation[]

  @@unique([tenant_id, reconciliation_number], map: "uq_trip_recon_number")
  @@unique([tenant_id, reconciliation_id], map: "uq_trip_recon_tenant_id")
  @@unique([tenant_id, trip_id], map: "uq_trip_recon_trip")
  @@index([tenant_id, status, started_at(sort: Desc)], map: "ix_trip_recon_status")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trip_staff {
  trip_staff_id       BigInt    @id @default(autoincrement())
  tenant_id           BigInt
  trip_id             BigInt
  staff_id            BigInt
  trip_role           String    @db.VarChar(30)
  joined_at           DateTime? @db.Timestamptz(6)
  left_at             DateTime? @db.Timestamptz(6)
  is_primary          Boolean   @default(false)
  assigned_by_user_id BigInt?
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  app_user            AppUser?  @relation(fields: [tenant_id, assigned_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_staff_assigned_by")
  staff               staff     @relation(fields: [tenant_id, staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_staff_staff")
  trip                Trip      @relation(fields: [tenant_id, trip_id], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_staff_trip")

  @@unique([tenant_id, trip_id, staff_id, trip_role, joined_at], map: "uq_trip_staff_assignment")
  @@unique([tenant_id, trip_staff_id], map: "uq_trip_staff_tenant_id")
  @@index([tenant_id, staff_id, joined_at], map: "ix_trip_staff_staff")
  @@index([tenant_id, trip_id], map: "ix_trip_staff_trip")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trip_stock_reconciliation {
  trip_stock_recon_id                                                        BigInt                @id @default(autoincrement())
  tenant_id                                                                  BigInt
  reconciliation_id                                                          BigInt
  inventory_location_id                                                      BigInt
  product_id                                                                 BigInt
  inventory_state_id                                                         BigInt
  expected_qty                                                               Decimal               @db.Decimal(14, 3)
  actual_qty                                                                 Decimal               @db.Decimal(14, 3)
  variance_qty                                                               Decimal?              @default(dbgenerated("(actual_qty - expected_qty)")) @db.Decimal(14, 3)
  variance_reason_code                                                       String?               @db.VarChar(50)
  variance_notes                                                             String?               @db.VarChar(1000)
  approval_status                                                            String                @default("NOT_REQUIRED") @db.VarChar(20)
  approved_by_user_id                                                        BigInt?
  approved_at                                                                DateTime?             @db.Timestamptz(6)
  adjustment_inventory_ledger_id                                             BigInt?
  counted_by_staff_id                                                        BigInt?
  counted_by_user_id                                                         BigInt?
  counted_at                                                                 DateTime              @default(now()) @db.Timestamptz(6)
  created_at                                                                 DateTime              @default(now()) @db.Timestamptz(6)
  inventory_ledger_entry                                                     InventoryLedgerEntry? @relation(fields: [tenant_id, adjustment_inventory_ledger_id], references: [tenantId, inventoryLedgerId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_adjustment")
  app_user_trip_stock_reconciliation_tenant_id_approved_by_user_idToapp_user AppUser?              @relation("trip_stock_reconciliation_tenant_id_approved_by_user_idToapp_user", fields: [tenant_id, approved_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_approved_by")
  staff                                                                      staff?                @relation(fields: [tenant_id, counted_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_counted_staff")
  app_user_trip_stock_reconciliation_tenant_id_counted_by_user_idToapp_user  AppUser?              @relation("trip_stock_reconciliation_tenant_id_counted_by_user_idToapp_user", fields: [tenant_id, counted_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_counted_user")
  trip_reconciliation                                                        trip_reconciliation   @relation(fields: [tenant_id, reconciliation_id], references: [tenant_id, reconciliation_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_header")
  inventory_location                                                         inventory_location    @relation(fields: [tenant_id, inventory_location_id], references: [tenant_id, inventory_location_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_location")
  product                                                                    Product               @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_product")
  inventory_state                                                            inventory_state       @relation(fields: [inventory_state_id], references: [inventory_state_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stock_recon_state")

  @@unique([tenant_id, reconciliation_id, inventory_location_id, product_id, inventory_state_id], map: "uq_trip_stock_recon_dimension")
  @@unique([tenant_id, trip_stock_recon_id], map: "uq_trip_stock_recon_tenant_id")
  @@index([tenant_id, reconciliation_id], map: "ix_trip_stock_recon_header")
  @@index([tenant_id, reconciliation_id, variance_qty], map: "ix_trip_stock_recon_variance")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trip_stop {
  trip_stop_id                                                           BigInt                     @id @default(autoincrement())
  tenant_id                                                              BigInt
  trip_id                                                                BigInt
  party_id                                                               BigInt
  route_stop_template_id                                                 BigInt?
  source                                                                 String                     @db.VarChar(30)
  sortable_order                                                         Decimal                    @db.Decimal(18, 6)
  status                                                                 String                     @default("PENDING") @db.VarChar(30)
  added_by_user_id                                                       BigInt?
  added_by_staff_id                                                      BigInt?
  added_at                                                               DateTime                   @default(now()) @db.Timestamptz(6)
  attempted_at                                                           DateTime?                  @db.Timestamptz(6)
  completed_at                                                           DateTime?                  @db.Timestamptz(6)
  reason_code                                                            String?                    @db.VarChar(50)
  notes                                                                  String?                    @db.VarChar(500)
  rescheduled_to_trip_stop_id                                            BigInt?
  created_at                                                             DateTime                   @default(now()) @db.Timestamptz(6)
  updated_at                                                             DateTime                   @default(now()) @db.Timestamptz(6)
  inventory_ledger_entry                                                 InventoryLedgerEntry[]
  money_ledger_entry                                                     MoneyLedgerEntry[]
  reconciliation_exception                                               reconciliation_exception[]
  stop_event                                                             StopEvent[]
  stop_follow_up_stop_follow_up_tenant_id_new_trip_stop_idTotrip_stop    stop_follow_up[]           @relation("stop_follow_up_tenant_id_new_trip_stop_idTotrip_stop")
  stop_follow_up_stop_follow_up_tenant_id_source_trip_stop_idTotrip_stop stop_follow_up[]           @relation("stop_follow_up_tenant_id_source_trip_stop_idTotrip_stop")
  staff                                                                  staff?                     @relation(fields: [tenant_id, added_by_staff_id], references: [tenant_id, staff_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_added_by_staff")
  app_user                                                               AppUser?                   @relation(fields: [tenant_id, added_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_added_by_user")
  party                                                                  Party                      @relation(fields: [tenant_id, party_id], references: [tenantId, partyId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_party")
  trip_stop                                                              trip_stop?                 @relation("trip_stopTotrip_stop", fields: [tenant_id, rescheduled_to_trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_rescheduled_to")
  other_trip_stop                                                        trip_stop[]                @relation("trip_stopTotrip_stop")
  route_stop_template                                                    route_stop_template?       @relation(fields: [tenant_id, route_stop_template_id], references: [tenant_id, route_stop_template_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_template")
  trip                                                                   Trip                       @relation(fields: [tenant_id, trip_id], references: [tenantId, tripId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_trip")
  trip_stop_product                                                      trip_stop_product[]

  @@unique([tenant_id, trip_stop_id], map: "uq_trip_stop_tenant_id")
  @@unique([tenant_id, trip_id, sortable_order], map: "uq_trip_stop_trip_order")
  @@index([tenant_id, party_id, trip_id], map: "ix_trip_stop_party_date_support")
  @@index([tenant_id, trip_id, status, sortable_order], map: "ix_trip_stop_trip_status_order")
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model trip_stop_product {
  trip_stop_product_id BigInt               @id @default(autoincrement())
  tenant_id            BigInt
  trip_stop_id         BigInt
  product_id           BigInt
  party_product_id     BigInt?
  quantity_mode        String               @db.VarChar(30)
  planned_qty          Decimal?             @db.Decimal(14, 3)
  forecast_qty         Decimal?             @db.Decimal(14, 3)
  price_snapshot       Decimal?             @db.Decimal(14, 2)
  status               String               @default("PENDING") @db.VarChar(20)
  notes                String?              @db.VarChar(500)
  created_at           DateTime             @default(now()) @db.Timestamptz(6)
  updated_at           DateTime             @default(now()) @db.Timestamptz(6)
  stop_event_product   stop_event_product[]
  party_product        party_product?       @relation(fields: [tenant_id, party_product_id], references: [tenant_id, party_product_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_product_party_product")
  product              Product              @relation(fields: [tenant_id, product_id], references: [tenantId, productId], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_product_product")
  trip_stop            trip_stop            @relation(fields: [tenant_id, trip_stop_id], references: [tenant_id, trip_stop_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_trip_stop_product_stop")

  @@unique([tenant_id, trip_stop_id, product_id], map: "uq_trip_stop_product_stop_product")
  @@unique([tenant_id, trip_stop_product_id], map: "uq_trip_stop_product_tenant_id")
  @@index([tenant_id, trip_stop_id, status], map: "ix_trip_stop_product_stop")
  @@schema("pms")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_role {
  tenant_id                                                  BigInt
  user_id                                                    BigInt
  role_id                                                    BigInt
  assigned_by_user_id                                        BigInt?
  assigned_at                                                DateTime @default(now()) @db.Timestamptz(6)
  app_user_user_role_tenant_id_assigned_by_user_idToapp_user AppUser? @relation("user_role_tenant_id_assigned_by_user_idToapp_user", fields: [tenant_id, assigned_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_user_role_assigned_by")
  role                                                       role     @relation(fields: [tenant_id, role_id], references: [tenant_id, role_id], onDelete: NoAction, onUpdate: NoAction, map: "fk_user_role_role")
  app_user_user_role_tenant_id_user_idToapp_user             AppUser  @relation("user_role_tenant_id_user_idToapp_user", fields: [tenant_id, user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_user_role_user")

  @@id([tenant_id, user_id, role_id])
  @@schema("pms")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model vehicle {
  vehicle_id         BigInt               @id @default(autoincrement())
  tenant_id          BigInt
  registration_no    String               @db.VarChar(50)
  vehicle_type       String               @db.VarChar(50)
  capacity           Decimal?             @db.Decimal(14, 3)
  capacity_unit      String?              @db.VarChar(30)
  ownership_type     String               @default("OWNED") @db.VarChar(30)
  status             String               @default("ACTIVE") @db.VarChar(20)
  notes              String?              @db.VarChar(500)
  created_by_user_id BigInt?
  created_at         DateTime             @default(now()) @db.Timestamptz(6)
  updated_at         DateTime             @default(now()) @db.Timestamptz(6)
  inventory_location inventory_location[]
  trip               Trip[]
  app_user           AppUser?             @relation(fields: [tenant_id, created_by_user_id], references: [tenantId, userId], onDelete: NoAction, onUpdate: NoAction, map: "fk_vehicle_created_by")
  tenant             Tenant               @relation(fields: [tenant_id], references: [tenantId], onDelete: NoAction, onUpdate: NoAction, map: "fk_vehicle_tenant")

  @@unique([tenant_id, registration_no], map: "uq_vehicle_tenant_registration")
  @@unique([tenant_id, vehicle_id], map: "uq_vehicle_tenant_vehicle")
  @@index([tenant_id, status], map: "ix_vehicle_tenant_status")
  @@schema("pms")
}
